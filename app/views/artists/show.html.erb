<h1> <%= "Show page for #{@artist.name}" %> </h1>
<p> <%= "My artist bio: '#{@artist.bio}'" %> </p>

<h5> Songs </h5>
<p> <% @artist.songs.each do |song| %>
<%= link_to "#{song.name}", song_path(song) %> </p>
<% end %>

<h5> Genres </h5>
<p> <% @artist.genres.uniq.each do |genre| %>
<%= link_to "#{genre.name}", genre_path(genre) %> </p>
<% end %>
<br>
<br>
<br>
<%= button_to "Edit Artist", edit_artist_path(@artist), {method: :get} %><br><br>
<%= button_to "Return to Artist Index", artists_path, {method: :get} %>